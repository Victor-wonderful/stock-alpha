/**
 * 작성 폼의 모양 — 서버 액션 파일("use server")에 둘 수 없다. 그 파일은 async 함수만
 * 내보낼 수 있어서, 상수를 같이 두면 빌드가 막힌다.
 *
 * 본문을 자유 서술로 두지 않고 세 칸으로 가른다. 「좋아 보인다」만 적힌 추천이 가장
 * 나쁘다. 특히 3번은 필수다 — 이 코너는 성적을 추적하지 않으므로, 접는 조건이 글에
 * 없으면 읽는 사람은 손을 뗄 시점을 영영 알 수 없다.
 */
export const BODY_SECTIONS = [
  { key: "why", label: "왜 지금인가", hint: "이번 주 실적에서 원가율이 3분기 만에 꺾였습니다" },
  { key: "basis", label: "무엇을 보고 있나", hint: "근거 2~3개" },
  {
    key: "invalidate",
    label: "무엇이 틀리면 접나",
    hint: "다음 분기에 원가율이 다시 올라가면 생각을 접습니다",
  },
] as const;

/** 엔진의 단기·중기·장기(5·10·20일)와 **다른 축**이다. 같은 이름을 쓰면 화면이 둘을 섞는다. */
export const HORIZONS = ["며칠", "몇 주", "몇 달"] as const;

export const MAX_SUMMARY = 120;
